<% if (books.length > 0) { %>
    <h2 class="page-header">Recently Added Books</h2>
<% } else { %>
    <h2 class="page-header">No books available</h2>
<% } %>
<%- include('partials/BookGrid', { books: books, large: true }) %>